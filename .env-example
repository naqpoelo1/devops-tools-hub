# === SECURITY ===
# Secret key for Flask sessions (use a long random string)
FLASK_SECRET_KEY=change-this-to-a-very-secure-random-string

# === UI CUSTOMIZATION (WHITE LABEL) ===
# App Name appearing in Header and Title
APP_TITLE=DevOps Tools Hub
# Short description on the landing page
APP_DESCRIPTION=Centralized platform for automation, monitoring, security, and developer utilities.
# URL or Path to App Logo (can be external URL or local path in /static/...)
APP_LOGO=/static/images/logo.png
# URL or Path to Favicon
APP_FAVICON=/static/images/favicon.png

# === LOGGING ===
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# JSON log format (suitable for Docker/ELK) or Plain Text (false)
USE_JSON_LOG=true

# === SONARQUBE API CONFIGURATION (FOR SCANNER) ===
# SonarQube Server API URL
SONAR_HOST_URL=https://sonarqube.example.com
# SonarQube User Token (Generate in User > Security)
SONAR_LOGIN_TOKEN=sqp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# (Optional) Default exclusions for scanner
SONAR_EXCLUSIONS=**/*_test.go,**/mocks/**,**/*.html
# Enable debug mode for sonar-scanner (true/false)
SONAR_DEBUG=false
# Minimum tokens for duplication check (CPD)
SONAR_CPD_MINIMUM_TOKENS=100

# === SONARQUBE WEB / SCREENSHOT CONFIG (FOR PLAYWRIGHT) ===
# URL for SonarQube Web Interface (usually same as SONAR_HOST_URL)
SONARQUBE_WEB_URL=https://sonarqube.example.com
# Credentials for automated screenshot login (Internal access)
SONAR_USERNAME=admin
SONAR_PASSWORD=password
# Cache duration for generated screenshots in hours
SCREENSHOT_TTL_HOURS=24

# === SONAR SCANNER PERFORMANCE & TUNING ===
# JVM Heap memory settings
SCANNER_HEAP_MIN=-Xms512m
SCANNER_HEAP_LIMIT=-Xmx2048m
# OS priority (0 to 19, higher means lower priority)
CPU_NICE_ADJUSTMENT=10
# Pin scanner to specific CPU cores (e.g., 0,1)
# CPU_AFFINITY=0,1

# === INTEGRATIONS & REDIRECTS ===
# URL for Repository Automation Frontend/Tool
REPO_AUTOMATION_FE_URL=https://repo-auto.example.com
# URL for File Compressor utility
FILE_COMPRESSOR_URL=https://compressor.example.com
# URL for Stirling PDF or PDF Studio (Alias: PDF_REDIRECT_URL)
STIRLING_STUDIO_URL=https://pdf.example.com/

# === GITHUB TOOLS ===

# Simple password to protect the GitHub Access Check page

GITHUB_ACCESS_PASSWORD=secret

# GitHub Personal Access Token (PAT) - Alternative to using ~/.netrc

GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx



# === FEATURE TOGGLES ===

# Enable or disable specific tools on the landing page (true/false)

ENABLE_REPO_SCANNER=true
ENABLE_STIRLING_PDF=true
ENABLE_REPO_AUTOMATION=true
ENABLE_FILE_COMPRESSOR=true
